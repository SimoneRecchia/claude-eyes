"""claudeEyes MCP server entry point.

NOT YET IMPLEMENTED. This is the scaffold — the MCP tools
(start_recording, stop_recording, list_frames, cleanup_session) will be
added in the next planning session.
"""

from __future__ import annotations

import sys


def main() -> None:
    print(
        "[claude-eyes] Server not yet implemented. "
        "See the design doc and planning session for the roadmap.",
        file=sys.stderr,
    )
    sys.exit(1)


if __name__ == "__main__":
    main()
